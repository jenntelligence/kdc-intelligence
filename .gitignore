node_modules
dist
.env
.env.local
*.log
.DS_Store
.vercel

# Tooling state (not part of project)
.omc/

# Local environment / secrets (NEVER commit)
.env.development.local
.env.test.local
.env.production.local
*.env

# Snowflake / DB credentials (defensive — should never be in repo anyway)
*.snowflake-credentials
snowflake.toml
*.pem
*.key

# Cached query results (may contain real customer data)
cache/
*.cache.json
data/exports/

# PR1 exploration query results — local dev only
explore-*.json

# PR3+ smoke test artifacts (large API response dumps)
smoke_result*.json
smoke_hotfix.json

# Session / harness artifacts (Claude Code, bkit, PDCA tracking)
.claude/
docs/.bkit-memory.json
docs/.pdca-status.json

# Refactor baseline snapshots — local verification artifacts only
snapshots/

# OS / IDE
Thumbs.db
.idea/
*.swp
